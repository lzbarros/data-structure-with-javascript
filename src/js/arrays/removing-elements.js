const popElement = (array =[]) => {
    array.pop()
    array.pop()
    array.pop()

    return array
}

const shiftElement = (array = []) => {
    array.shift()
    array.shift()
    array.shift()

    return array
}